{
  "name": "waline-comment-server",
  "version": "1.0.0",
  "description": "Waline 评论系统服务端",
  "main": "api/index.js",
  "scripts": {
    "start": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@waline/vercel": "^1.41.5"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "overrides": {
    "jsdom": "27.3.0",
    "html-encoding-sniffer": "4.0.0"
  },
  "_overridesNote": "jsdom>=27.4.0 依赖 html-encoding-sniffer@6 -> @exodus/bytes(纯ESM)，CJS require 抛 ERR_REQUIRE_ESM 导致函数启动即 500。锁 jsdom 27.3.0 保持纯 CJS 依赖链。"
}
